apiVersion: batch/v1
kind: Job
metadata:
  name: hello-world-job
spec:
  template:
    spec:
      containers:
      - name: hello-world
        image: busybox
        command: ['sh', '-c', 'echo "hello world"; sleep 30']
      restartPolicy: Never
