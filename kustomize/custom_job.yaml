apiVersion: batch/v1
kind: Job
metadata:
  name: hello-world-job
spec:
  template:
    spec:
      containers:
      - name: hello-world
        image: busybox
        command: ['sh', '-c', 'echo "Good Word!! hello world"; sleep 20']
      restartPolicy: Never
